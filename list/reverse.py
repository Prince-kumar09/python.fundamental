#1️⃣ reverse() — sirf order ulta karta hai
numbers = [10, 20, 30, 40, 50]

numbers.reverse()

print(numbers)
#3️⃣ sort(reverse=True) — descending order mein sort karta hai
numbers = [30, 10, 50, 20, 40]

numbers.sort(reverse=True)# short in decending order

print(numbers)
